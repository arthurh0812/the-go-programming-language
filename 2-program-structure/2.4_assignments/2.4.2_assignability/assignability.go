package main

func main() {
	/*
		An assignment, implicit or explicit, is always legal if the left-hand
		side (the variable) and the right-hand side (the value) have the same
		type. The assignment is legal only if the value is assignable to the
		type of the variable.
	*/

	/*
		In any comparison, the first operand must be assingable to the type
		of the second operand, or vice versa.
	*/
}
